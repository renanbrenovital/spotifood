import styled from 'styled-components';

export const Organism = styled.aside`
  background: var(--color-background-dark);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-evenly;
`;